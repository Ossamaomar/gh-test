name: scheduled workflow

on:
    schedule:
        - cron: "0 0 * * *"
jobs:
    daily-job:
        runs-on: ubuntu-latest
        steps:
            - name: Print message
              run: echo "Hello scheduled"    
    